import styled from 'styled-components'
import { space, position, width, height } from 'styled-system'

export const Div = styled.div`
  ${space}
  ${position}
  ${width}
  ${height}
`

export default Div
